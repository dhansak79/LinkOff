# openspec/specs

These spec files are **rendered reference outputs** from the `spec-change` swamp model. They are not the source of truth.

The authoritative state for each change lives in `.swamp/spec-change-{name}.json`. Feature files (the executable form of the scenarios) live in `tests/cucumber/features/`.

Pre-migration specs authored under the original OpenSpec workflow are preserved here as reference material.
