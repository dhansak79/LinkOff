#!/bin/sh
SWAMP="${HOME}/.swamp/bin/swamp"
command -v swamp >/dev/null 2>&1 && SWAMP="swamp"

# Skip if all staged changes are in telemetry/
git diff --cached --name-only | grep -qv '^telemetry/' || exit 0
"$SWAMP" workflow run quality-gate-fast
